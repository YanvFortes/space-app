import { styled } from "styled-components"

const BannerStyled = styled.img`
    
`

const Banner = () => {
    return(
        <BannerStyled
            src="/img/banner.png"
        />
    )
}

export default Banner